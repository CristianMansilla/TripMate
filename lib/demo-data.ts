import { Activity, Expense, PackingItem, Reservation, Trip } from './types'

// El modo demo comienza vacío. Los viajes reales se cargan únicamente desde
// la cuenta autenticada y nunca se publican como plantillas compartidas.
export const trips:Trip[]=[]
export const activities:Activity[]=[]
export const reservations:Reservation[]=[]
export const expenses:Expense[]=[]
export const packing:PackingItem[]=[]
